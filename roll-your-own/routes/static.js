fs = require('fs');
module.exports = function staticHandler(url, cb){
  //read file
  //callback:
  //  if err cb(err)
  //  else cb(results: data.toString('utf8')

  //results = {
    //body : 'you requested + ' + url.pathname
  //};
  //process.nextTick(function(){
    //cb(null, results);
  //});
};

